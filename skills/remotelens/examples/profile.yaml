# Repository-owned example. Replace every path and value locally.
api_base_url: http://localhost:3000/api/v1
cv_path: /absolute/path/to/the/cv-you-selected.md

profile:
  skills:
    - TypeScript
    - React
    - distributed systems
  preferred_countries:
    - JP
  employment_types:
    - full_time
  seniority: senior
  work_authorization: unknown
  travel_preference: no

matching:
  max_jobs: 25
  include_closed: false
